{
  "compilerOptions": {
    "target": "ES6", // Определяет версию ECMAScript, которая будет использоваться для компиляции. Например, ES5, ES6, ESNext.
    "module": "commonjs", // Определяет систему модулей. Например, CommonJS, ESNext.
    "checkJs": true, // Включает проверку типов для файлов JavaScript.
    "jsx": "react", // Определяет как обрабатывать JSX в файлах .tsx и .tsx.
    "baseUrl": ".", // Базовый URL для разрешения путей.
    "paths": { // Указывает псевдонимы для путей.
      "@components/*": ["src/components/*"],
      "@utils/*": ["src/utils/*"]
    },
    "strict": true, // Включает все строгие проверки типов.
    "esModuleInterop": true, // Включает совместимость с ES модулями для импорта CommonJS модулей.
    "allowSyntheticDefaultImports": true, // Позволяет импортировать по умолчанию из модулей, которые не поддерживают экспорт по умолчанию.
    "skipLibCheck": true, // Пропускает проверку типов в файлах библиотек.
    "noEmit": true, // Запрещает генерацию выходных файлов.
    "resolveJsonModule": true // Позволяет импортировать файлы JSON как модули.
  },
  "include": [
    "src/**/*" // Указывает файлы, которые должны быть включены в проект.
  ],
  "exclude": [
    "node_modules", // Указывает файлы и директории, которые должны быть исключены из проекта.
    "**/*.spec.ts",
    "*.tsx",
  ]
}
